// Imported action creators



// Initial State
const initialState = {
    data: ['test'],
    fetching: false,
    error: null,
}

const reducer = (sate = initialState, action) => {
    switch(action.type) {

        default:
            return state;
    }
};

export default reducer;